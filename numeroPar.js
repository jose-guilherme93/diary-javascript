function verificarNumero(number) {
    while (number < 10 ) {
        console.log(number)
        number++
    }
let result = verificarNumero(0)



 console.log(result)
} 
return verificarNumero()

